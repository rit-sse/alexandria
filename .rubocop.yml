AllCops:
  Includes:
    - Rakefile
    - config.ru
  Excludes:
    # TODO: Only exclude LCCAble for the RedundantSelf cop.
    #app/models/concerns/lccable.rb
    - db/**
    #- config/**
    #- script/**

# This configuration was generated by `rubocop --auto-gen-config`.
# The point is for the user to remove these configuration records
# one by one as the offences are removed from the code base.

LineLength:
  Enabled: false

MethodLength:     # Seperated out as much as seemed reasonable. 3 methods have 11 lines
  Enabled: false  # and one has 15 but it would be unreasonable to seperate out more logic.

SpaceInsideHashLiteralBraces:
  Enabled: false
